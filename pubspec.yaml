name: neom_generator
description: Neom Generator for Open Neom.
version: 1.3.0-dev
homepage: https://github.com/Open-Neom/neom_generator

publish_to: 'none'
environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  neom_core: ^1.4.1-dev
  neom_commons: ^1.4.0-dev

#  FUTURE EXPERIMENTAL USE:
#  neom_vr:
#    git:
#      url: git@github.com:Cyberneom/neom_vr.git

  sleek_circular_slider: ^2.0.1

  #Audio Processing Experimental
  surround_frequency_generator: ^1.0.0

  #AudioProcessing
  flutter_sound: ^9.28.0
  pitch_detector_dart: ^0.0.7

  #Tonic is a Flutter-compatible Dart package that models components of music theory.
  #Used for Neom Harmony
  #tonic: ^0.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
